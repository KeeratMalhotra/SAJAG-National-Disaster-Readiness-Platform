<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Session QR Code</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .qr-container {
            border: 8px solid #000;
            padding: 20px;
            display: inline-block;
            background: white;
        }
        @media print {
            .no-print { display: none !important; }
            body { background: white; }
        }
    </style>
</head>
<body class="bg-light text-center py-5">

    <div class="container">
        <div class="mb-4">
            <h1 class="display-5 fw-bold text-uppercase"><%= training.theme %> Training</h1>
            <h3 class="text-muted"><%= training.location_text %></h3>
        </div>

        <div class="qr-container mb-4 shadow">
            <img src="<%= qrCodeImage %>" alt="Scan to Start" style="width: 300px; height: 300px;">
        </div>

        <div class="card mx-auto shadow-sm" style="max-width: 600px;">
            <div class="card-body">
                <h4 class="card-title text-danger fw-bold">
                    <i class="bi bi-shield-lock"></i> Fraud Protection Active
                </h4>
                <ul class="text-start mt-3">
                    <li>Participants must be within <strong>200 meters</strong> of this location.</li>
                    <li>This QR code will <strong>expire</strong> when the training ends.</li>
                    <li>If GPS fails, please ask the Volunteer for help.</li>
                </ul>
                <div class="alert alert-primary mt-3">
                    <strong>Session Code:</strong> <span class="fs-4 fw-bold mx-2"><%= training.session_code || 'N/A' %></span>
                </div>
            </div>
        </div>

        <div class="mt-5 no-print">
            <button onclick="window.print()" class="btn btn-lg btn-dark me-2">🖨️ Print Poster</button>
            <a href="/trainings/<%= training.id %>" class="btn btn-lg btn-outline-secondary">⬅️ Back to Dashboard</a>
        </div>
    </div>

</body>
</html>