<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
    <link href='https://api.mapbox.com/mapbox-gl-js/v2.9.1/mapbox-gl.css' rel='stylesheet' />

    <link rel="stylesheet" href="/css/sidebar.css" />
    <link rel="stylesheet" href="/css/sdma-dashboard.css" /> 
    <link rel="stylesheet" href="/css/map.css" />
</head>

<body class ="p-3">
    <%- include('../partials/_sidebar') %>

   <main class="main-content">
        <div class="identity-header">
            <img src="/images/ndma.png" alt="NDMA Logo" class="logo">
            <div class="org-details">
                <h1 class="org-name"><%= state %> Disaster Management Authority</h1>
                <p class="org-subtitle">State Level Dashboard</p>
            </div>
            <img src="/images/<%= state.toLowerCase().replace(/ & /g, '-').replace(/\s+/g, '-') %>-logo.png" alt="<%= state %> Logo" class="logo state-logo" onerror="this.style.display='none'">
        </div>

        <div class="page-actions mb-3"> <div class="action-group">
                <button class="btn btn-theme-secondary" data-bs-toggle="modal" data-bs-target="#importModal"><i class="bi bi-upload me-2"></i>Bulk Import</button>
                <button id="downloadReportBtn" class="btn btn-theme-secondary"><i class="bi bi-download me-2"></i>Download Report</button>
            </div>
            <button class="btn btn-theme-primary" data-bs-toggle="modal" data-bs-target="#announcementModal"><i class="bi bi-plus-circle-fill me-2"></i>New Announcement</button>
        </div>
        
        <div id="report-content">
            <% if (locals.announcements && announcements.length > 0) { %>
                <div class="announcement-bar alert alert-dismissible fade show mb-3"> <i class="bi bi-megaphone-fill icon"></i>
                    <div class="content">
                        <strong><%= announcements[0].title %>:</strong>
                        <span><%= announcements[0].content %></span>
                    </div>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            <% } %>

            <div class="row g-3 mb-3"> <div class="col-md-6 col-xl-4">
                    <div class="kpi-card h-100">
                        <div class="kpi-icon icon-blue"><i class="bi bi-card-checklist"></i></div>
                        <div class="kpi-content">
                            <h6 class="kpi-label">Total Trainings</h6>
                            <p class="kpi-value"><%= trainings.length %></p>
                        </div>
                    </div>
                </div>
                <div class="col-md-6 col-xl-4">
                    <div class="kpi-card h-100">
                        <div class="kpi-icon icon-green"><i class="bi bi-people-fill"></i></div>
                        <div class="kpi-content">
                            <h6 class="kpi-label">Active Partners</h6>
                            <p class="kpi-value">N/A</p>
                        </div>
                    </div>
                </div>
                <div class="col-md-12 col-xl-4">
                    <div class="kpi-card h-100">
                        <div class="kpi-icon icon-red"><i class="bi bi-bullseye"></i></div>
                        <div class="kpi-content">
                            <h6 class="kpi-label">Readiness Score</h6>
                            <p class="kpi-value">N/A</p>
                        </div>
                    </div>
                </div>
            </div>

            <div class="row g-3"> <div class="col-lg-7">
                    <div class="dashboard-card h-100 d-flex flex-column">
                        <div class="card-title-bar">
                            <h5 class="card-title"><i class="bi bi-geo-alt-fill me-2"></i><%= state %> Training Map</h5>
                        </div>
                        <div class="card-body-content p-3 flex-grow-1 d-flex flex-column">
                            <div class="btn-group mb-3" role="group" id="map-filter-group">
                                <button type="button" class="btn btn-sm btn-outline-primary active" data-filter="All">All</button>
                                <button type="button" class="btn btn-sm btn-outline-secondary" data-filter="Earthquake">Earthquake</button>
                                <button type="button" class="btn btn-sm btn-outline-secondary" data-filter="Flood">Flood</button>
                                <button type="button" class="btn btn-sm btn-outline-secondary" data-filter="Cyclone">Cyclone</button>
                                <button type="button" class="btn btn-sm btn-outline-secondary" data-filter="Landslide">Landslide</button>
                            </div>
                            <div id="map" 
                                 data-token="<%= MAPBOX_ACCESS_TOKEN %>"
                                 data-state="<%= state %>"
                                 class="flex-grow-1" 
                                 style="border-radius: 8px;">
                            </div>
                        </div>
                    </div>
                </div>

                <div class="col-lg-5">
                    <div class="dashboard-card h-100 d-flex flex-column">
                        <div class="card-title-bar">
                            <h5 class="card-title"><i class="bi bi-table me-2"></i>Training Program List</h5>
                        </div>
                        <div class="card-body-content flex-grow-1 p-0" style="overflow-y: hidden;">
                            <div class="table-responsive h-100">
                                <table class="table training-table table-hover mb-0">
                                    <thead>
                                        <tr>
                                            <th>Title</th>
                                            <th>Theme</th>
                                            <th>Organization</th>
                                            <th>Start Date</th>
                                            <th>Location</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% if (trainings.length > 0) { %>
                                            <% trainings.forEach(training => { %>
                                                <tr>
                                                    <td><%= training.title %></td>
                                                    <td><span class="badge theme-badge"><%= training.theme %></span></td>
                                                    <td><%= training.organization_name %></td>
                                                    <td><%= new Date(training.start_date).toLocaleDateString('en-GB', { day: '2-digit', month: 'short', year: 'numeric' }) %></td>
                                                    <td><%= training.location_text %></td>
                                                </tr>
                                            <% }) %>
                                        <% } else { %>
                                            <tr>
                                                <td colspan="5" class="text-center p-5">
                                                    <p class="text-muted mb-0">No Training Programs Found</p>
                                                </td>
                                            </tr>
                                        <% } %>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>
    
    <div class="modal fade" id="announcementModal" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Create New Announcement</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <form id="announcementForm">
                        <div class="mb-3">
                            <label for="title" class="form-label">Title</label>
                            <input type="text" class="form-control" id="title" name="title" required>
                        </div>
                        <div class="mb-3">
                            <label for="content" class="form-label">Content</label>
                            <textarea class="form-control" id="content" name="content" rows="3" required></textarea>
                        </div>
                        <div id="announcement-message" class="mt-3"></div>
                        <div class="d-flex justify-content-end mt-3">
                            <button type="button" class="btn btn-secondary me-2" data-bs-dismiss="modal">Close</button>
                            <button type="submit" class="btn btn-primary">Post Announcement</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
    
    <div class="modal fade" id="importModal" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Bulk Import Training Data</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                     <p>Upload a CSV file with historical training data. The file must match the template format.</p>
                     <a href="/trainings_template.csv" class="btn btn-sm btn-outline-secondary mb-3" download><i class="bi bi-filetype-csv"></i> Download CSV Template</a>
                     <hr>
                    <form id="importForm" enctype="multipart/form-data">
                        <div class="input-group">
                            <input type="file" class="form-control" name="trainingsCsv" accept=".csv" required>
                            <button class="btn btn-primary" type="submit">Upload</button>
                        </div>
                    </form>
                      <div id="import-message" class="mt-3"></div>
                </div>
            </div>
        </div>
    </div>
    <script src='https://unpkg.com/@turf/turf@6/turf.min.js'></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js"></script>
    <script src='https://api.mapbox.com/mapbox-gl-js/v2.9.1/mapbox-gl.js'></script>

    <script src="/js/reporting.js"></script>
    <script src="/js/import.js"></script>
    <script src="/js/sdma-map.js"></script> 
    <script src="/js/announcement.js"></script>
</body>
</html>