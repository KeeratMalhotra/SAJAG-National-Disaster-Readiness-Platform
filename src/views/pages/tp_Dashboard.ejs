<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .card-link {
            text-decoration: none;
            color: inherit;
        }
        .card-link .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 4px 20px rgba(0,0,0,0.1);
            transition: all 0.2s ease-in-out;
        }
    </style>
</head>
<body class="bg-light">

    <%- include('../partials/_navbar') %>

    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2>Your Training Programs</h2>
            <a href="/trainings/new" class="btn btn-primary btn-lg">
                + Create New Training
            </a>
        </div>

        <% if (trainings.length > 0) { %>
            <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
                <% trainings.forEach(training => { %>
                    <div class="col">
                        <a href="/trainings/<%= training.id %>" class="card-link">
                            <div class="card h-100">
                                <div class="card-header bg-secondary text-white">
                                    <%= training.theme %>
                                </div>
                                <div class="card-body">
                                    <h5 class="card-title"><%= training.title %></h5>
                                    <p class="card-text"><%= training.location_text %></p>
                                </div>
                                <div class="card-footer">
                                    <small class="text-muted"><%= new Date(training.start_date).toLocaleDateString() %></small>
                                </div>
                            </div>
                        </a>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <div class="card">
                <div class="card-body text-center p-5">
                    <h4>You haven't created any training programs yet.</h4>
                    <p>Click the "+ Create New Training" button to get started!</p>
                </div>
            </div>
        <% } %>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>