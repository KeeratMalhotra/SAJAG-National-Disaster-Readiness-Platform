<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />

    <style>
        #map { height: 600px; } /* Give the map a defined height */
    </style>
</head>
<body class="bg-light">

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/dashboard">SAJAG National Dashboard</a>
            <div class="d-flex">
                <span class="navbar-text me-3">Welcome, <%= user.name || 'Admin' %></span>
                <a href="/api/auth/logout" class="btn btn-outline-light">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row mb-4">
            <div class="col-md-4">
                <div class="card text-center">
                    <div class="card-body">
                        <h5 class="card-title">Total Trainings Conducted</h5>
                        <p class="fs-2 fw-bold"><%= totalTrainings %></p>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card text-center">
                    <div class="card-body">
                        <h5 class="card-title">Active Training Partners</h5>
                        <p class="fs-2 fw-bold"><%= uniquePartners %></p>
                    </div>
                </div>
            </div>
             <div class="col-md-4">
                <div class="card text-center text-bg-primary">
                    <div class="card-body">
                        <h5 class="card-title">National Readiness Score</h5>
                        <p class="fs-2 fw-bold"><%= averageScore %>%</p>
                    </div>
                </div>
            </div>
            </div>

        <div class="card">
            <div class="card-header">
                National Training Map
            </div>
            <div class="card-body">
                <div id="map"></div>
            </div>
        </div>
        <div class="card mt-4">
            <div class="card-header">
                Priority Training Gaps
            </div>
            <div class="card-body">
                <% if (gaps.length > 0) { %>
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>District</th>
                                <th>Disaster Theme</th>
                                <th>Reason</th>
                                <th>Priority</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% gaps.forEach(gap => { %>
                                <tr>
                                    <td><%= gap.district %></td>
                                    <td><%= gap.theme %></td>
                                    <td><%= gap.reason %></td>
                                    <td><span class="badge bg-danger"><%= gap.priority %></span></td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                <% } else { %>
                    <p class="text-center text-success">No critical training gaps detected. Well done!</p>
                <% } %>
            </div>
        </div>
    </div>

    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
    <script src="/js/map.js"></script>
    </body>
</html>